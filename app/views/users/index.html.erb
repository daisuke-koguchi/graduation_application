<div class="container-fluid">
  <div class="row">
    <div class="col-12 col-md-6 comunity-logo">
      <div class="d-flex flex-column">
        <div style="height:50px"></div>
        <div class="d-flex justify-content-center" style="height:100px"><h2><%= current_user.nick_name %>さんのコミュニティ</h2></div>
        <div style="col-0 height:50px"></div>
      </div>
    </div>
    <div class="col-12 col-md-6 form-group">
      <%= search_form_for @q do |f|%>
        <%= f.label :nick_name %>
        <%= f.search_field :nick_name_cont, class: "form-control form-control-lg ",placeholder: "ニックネームを入力" %>
        <small class="form-text">登録されたニックネームでご利用者さまを検索できます。</small>
        <%= f.submit "検索",class:'btn btn-secondary btn-lg'%>
      <% end %>
    </div>
    <div class="col-12">
      <div class="card">
        <div class="card community-card">
        <h4 class="card-title">コミュニティ</h4>
        <h5 class="card-text">こちらではご登録されているご利用者さまのプロフィールを確認することができます。</h5>
        <h5 class="card-text">相手のプロフィールを確認するときは、プロフィールを確認するボタンを押してみましょう。</h5>
        <h5 class="dard-text">メッセージを確認するボタンを押すと、相手とメッセージをやりとりすることができます。</h5>
        <h5 class="dard-text">ニックネームのリンクを押すとをニックネームを昇順・降順（昇順▲ /降順▼）で並び替えられます。</h5>
        <div class="table-responsive-md">
          <table class="table text-nowrap">
            <thead>
              <tr>
                <th style="width: 30%">プロフィール画像</th>
                <th style="width: 20%"><%= sort_link(@q, :nick_name,"ニックネーム") %></th>
                <th style="width: 25%">プロフィールを確認する</th>
                <th style="width: 25%" >メッセージを確認する</th>
              </tr>
            </thead>
            <tbody>
              <% @users.each do |user| %>
              <% if current_user.id != user.id %>
                <tr>
                  <td class="d-flex ml-5">
                    <% if user.avatar_image?%>
                      <%= image_tag(user.avatar_image.thumb.url) %>
                    <% else %>
                      <%= image_tag('noimage.png',width:'40%',height: '40%')%>
                    <% end %>
                  </td>
                  <td id="user_nick_name"><%= user.nick_name%>さん</td>
                    <% if user.id != current_user.id %>
                  <td><%= link_to "プロフィールを確認する ", user ,class:'btn btn-info btn-lg '%></td>
                  <td><%= link_to 'メッセージを確認する', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: :post ,class:'btn btn-primary  btn-lg'%></td>
                  <% end%>
                </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>
          <%= paginate @users %>
          </div>
        </div>
      </div>
    </div>
    <div class="col-12 d-flex mt-3 mb-3 pt-1 pt-1">
      <%= link_to "戻る",user_path(id: current_user.id),class:'btn btn-success btn-lg' %>
    </div>
  </div>
</div>