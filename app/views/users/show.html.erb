<div class="container-fluid">
  <div class="row">
    <div class="col-12 pt-5">
    <% if @user.id  == current_user.id %>
      <h2 class="user_title"><%= @user.nick_name %>さんのマイページ</h2>
    <% else %>
      <h2 class="user_title"><%= @user.nick_name%>さんのプロフィール</h2>
    <% end %>
    </div>
    <div class="col-12 col-md-8 offset-md-2 pl-5">
      <div class="d-flex justify-content-start">
        <% if @user.avatar_image? %>
          <%= image_tag(@user.avatar_image.url) if @user.avatar_image && @user.avatar_image.url %>
        <% else %>
          <%= image_tag('noimage.png',width:'60%',height:'60%')%>
        <% end %>
      </div>
    </div>
    <div class="col-12 col-md-8 offset-md-2">
      <div class="table-responsive">
        <table class="table">
          <tr>
            <th class=" text-nowrap" style="width: 20%" >ニックネーム</th>
            <td style="width: 60%"><%= @user.nick_name%> さん</td>
          </tr>
          <tr>
            <th class=" text-nowrap">新規会員登録日</th>
            <td><%= @user.created_at.strftime("%Y年%-m月%d日") %></td>
          </tr>
          <tr>
            <th >自己紹介</th>
            <td id="introduction"><%= @user.introduction%></td>
          </tr>
        <% if @user.id  == current_user.id %>
          <tr>
            <th class=" text-nowrap">プロフィールを編集する</th>
            <td><%= link_to "プロフィールを編集する", edit_user_registration_path, class: "btn btn-secondary btn-lg"%></td>
          </tr>
          <tr>
            <th class=" text-nowrap">Rehamoを退会する</th>
            <td><%= link_to "退会する", user_registration_path, method: :delete, data:{confirm: "アカウントを削除してもよろしいですか？"}, class:"btn btn-outline-danger btn-lg"%></td>
          </tr>
        <% end %>
        </table>
      </div>
    </div>
  </div>
  <% if @user.id  == current_user.id %>
  <div class="row">
    <div class="card-group m-5">
      <div class="col-lg-12 card">
      <h4 class="card-header">運動する・記録する</h4>
        <div class="card-body">
          <div class="exercise"></div>
          <h5>　運動をするとき、運動の記録をするときは、「運動する・記録する」ボタンを押してください。運動ページへ移動します。</h5>
          <div class="d-flex justify-content-center">
          <%= link_to "運動する・記録する", exercises_path, class:'btn btn-info btn-lg'%>
          </div>
        </div>
      </div>
      <div class="col-lg-12 card">
        <h4 class="card-header">仲間と交流する</h4>
        <div class="card-body">
        <div class="community"></div>
        <h5>　仲間とメッセージをするときは、「仲間と交流する」ボタンを押してください。コミュニティページへ移動します。</h5>
        <div class="d-flex justify-content-center">
          <%= link_to "仲間と交流する" , users_path, class:'btn btn-primary btn-lg'%>
        </div>
        </div>
      </div>
    </div>
  </div>
  <% end %>
  <% if @user.id  != current_user.id %>
  <div class="row">
    <div class="ol-md-8 offset-md-2 ">
      <%= link_to "戻る", users_path, class: "btn btn-success btn-lg"%>
    </div>
  <% end %>
  </div>
</div>


