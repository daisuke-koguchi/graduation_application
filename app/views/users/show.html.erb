<h2>プロフィール</h2>

<div>
  <h2>プロフィール画像</h2>
  <%= image_tag(@user.avatar_image.url) if @user.avatar_image && @user.avatar_image.url %>
</div>

<div>
  <h2>氏:<%= @user.first_name %> 名:<%= @user.last_name %></h2>
</div>

<div>
  <h2>メールアドレス</h2>
  <%= @user.email %>
</div>

<div>
  <h2>自己紹介</h2>
  <%= @user.introduction%>
</div>

<%= link_to "戻る", users_mypage_path(id: current_user.id) %>
