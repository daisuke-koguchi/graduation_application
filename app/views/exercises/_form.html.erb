<div class="col-12 ">
  <%= form_with(model:@exercise,local: true) do |f|%>
  <%= render 'layouts/error_messages', model: f.object %>
  <div class="table-responsive">
    <table class="table text-nowrap form-group">
      <tbody>
        <tr>
          <th class="text-nowrap" style="width: 30%"><%= f.label :name %><span class="badge badge-danger" style="font-size: 13px;">必須<span></th>
          <td><%= f.text_field :name, class: "form-control form-control-lg",placeholder: "例) スクワット" %></td>
        </tr>
        <tr class="dexcription">
          <th class="text-nowrap" ><%= f.label :description %><span class="badge badge-danger" style="font-size: 13px;">必須<span></th>
          <td><%= f.text_area :description, class: "form-control form-control-lg",placeholder: "例) スクワットをする際は、
          ①両足を肩幅まで広げること。
          ②お尻を落としながら、膝を指先より出ない程度までを曲げること。
          この２点に注意してやってみましょう!!"%></td>
        </tr>
        <tr>
          <th class="text-nowrap" style="font-size: 28px;">運動負荷<span class="badge badge-secondary" style="font-size: 13px;">任意<span></th>
          <td>運動回数: <%= f.number_field :count, class: "form-control"%>
              セット数: <%= f.number_field :set_count, class: "form-control"%>
              分: <%= f.number_field :minute, class: "form-control"%>
              秒: <%= f.number_field :second, class: "form-control" %>
          </td>
        </tr>
        <tr>
          <th><%= f.label :image %><span class="badge badge-secondary" style="font-size: 13px;">任意<span></th>
          <td><%= f.file_field :image %></td>
        </tr>
        <tr>
          <th><%= f.label :video %><span class="badge badge-secondary" style="font-size: 13px;">任意<span></th>
          <td><%= f.file_field :video, :accept => 'video/*'%></td>
        </tr>
        <tr>
          <th style="font-size: 28px;">日付<span class="badge badge-danger" style="font-size: 13px;">必須<span></th>
          <td>
            <%= f.fields_for :schedules do |s| %>
              <%= render 'schedule_fields', f: s %>
            <% end %> 
            <div class="links">
              <%= link_to_add_association "日付を追加", f, :schedules %>
            </div>  
          </td>
        </tr>
      </tbody>
    </table>
  </td>
    <div class="d-flex justify-content-start">
      <div class="m2 p-3"><%= link_to "戻る", :back , class: 'btn btn-success btn-lg'%></div>
      <div class="m2 p-3"><%= f.submit "登録する", class: 'btn btn-primary btn-lg'%></div>
    </div>
  <% end %>
</div>




